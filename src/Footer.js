import React from 'react';

const Footer = () => {
    return(
        <div>
            <form>
                <input type={'search'}/>
                <button type={'submit'}>ADD</button>
            </form>
        </div>
    )
}
export default Footer